/*
  validate the config
   - check that definitions are logically sound
   - check that can connect to the db with the connectionConfig
   - check that db connection has sufficient privilages
   - check that the language and dialect are supported
*/
