#!/usr/bin/env node

require('tsx/cjs'); // register tsx so that require() method can import typescript files as well as js

require('@oclif/core')
  .run()
  .then(require('@oclif/core/flush'))
  .catch(require('@oclif/core/handle'));
